{
    "build": {
      "image": "node:alpine",
      "command": "yarn install && yarn run build && echo $SOME_ENV",
      "publicDir": "public",
      "baseDir": "packages/frontend",
      "environment": {
        "SOME_ENV": "Build finished!"
      }
    }
   }
   